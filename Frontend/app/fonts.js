import {Carter_One} from "next/font/google";
import {Rancho} from "next/font/google";
import {Ubuntu} from "next/font/google";

export const ubuntu = Ubuntu({ subsets: ["latin"],weight:['400'], variable:"--font-ubuntu" });
export const carter = Carter_One({ subsets: ["latin"],weight:['400'], variable:"--font-carter" });
export const rancho = Rancho({ subsets: ["latin"],weight:['400'], variable:"--font-rancho" });
